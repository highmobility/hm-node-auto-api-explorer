<%if (typeof label == "undefined") { %>
    <%= value %>
    <% }  else if (value !== null && value !== undefined) { %>
    <div class="property">
        <span><%= label %></span>
        <span><%= value %> <%= unit %></span>
    </div>
<% } %>


